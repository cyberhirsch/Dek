{
  "name": "dek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A minimal, single-file presentation editor: edit slides in code, via an LLM, or WYSIWYG in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vue-tsc": "^2.2.0"
  }
}
