node_modules
dist
*.local
.DS_Store
.vite
*.tsbuildinfo
.claude/

# Slide content stays local / private — the repo ships the *tool*, not the lectures.
/deck.md
/decks/
/public/Assets/*
# …but the tutorial screenshots used by deck.example.md are committed.
!/public/Assets/tutorial/
# (deck.example.md is committed as a runnable starting point)
